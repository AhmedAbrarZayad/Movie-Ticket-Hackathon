{
  "pages": [
    {
      "text": "CinemaSeat — architecture & data pipeline\nThis document explains what the system does at each stage, how it's implemented, and\nwhy that choice was made over the alternatives. It's written to double as the architecture\nsection of the hackathon README and as a reference for the team while building.\nStack: React (frontend) · Node.js / Express modular monolith (backend) · PostgreSQL\n(source of truth) · Redis (locks, OTP, rate limits, job queue) · BullMQ (background workers) ·\nprovided mock payment/OTP gateway.\n0. The one idea that repeats everywhere\nReject cheap, reject early. Every layer of this system is ordered so that the least expensive\ncheck happens first, and a request only reaches the expensive, contended resource\n(Postgres) after it has survived everything cheaper. You'll see this pattern three times: the\nmiddleware stack (rate limit → auth → validation, before any handler logic runs), the seat-\nhold flow (Redis lock before Postgres transaction), and the webhook handler (idempotency\ncheck before any business logic). It's the same principle applied at three different layers,\nand it's worth stating explicitly in DECISIONS.md — it's the reasoning that ties the whole\ndesign together.",
      "num": 1
    },
    {
      "text": "1. Request lifecycle — the full path of one HTTP request\nWhat: every request passes through the same ordered middleware chain before it ever\nreaches a handler. How: helmet (security headers) → cors → rate limiter (Redis-backed) →\nbody parser with a size cap → JWT auth check → route-specific schema\nvalidation/sanitization → handler. Why this order specifically: each step is cheaper than\nthe one after it. A rate-limited or unauthenticated request is rejected in microseconds\nwithout ever touching a validation schema or the database. Doing validation before auth, or\nauth before rate limiting, would mean paying the cost of the more expensive check for\ntraffic you were always going to reject anyway.\n2. Frontend validation\nWhat: format and completeness checks on forms — email/phone format, required fields,\nseat selection limits, OTP code shape — before a request is even sent.\nHTTPS request\nwithin limits\nvalid token\nschema valid\nenqueue job\nexceeded\ninvalid/missing token\nschema fails\nReact client\nRate limiter\nAuth middleware\nValidation and sanitization\nRoute handler / business\nlogic\nPostgreSQL \tRedis BullMQ worker\n429 Too Many Requests\n401 Unauthorized\n400 Bad Request",
      "num": 2
    },
    {
      "text": "How: schema-based validation (Zod or Yup) wired into the form layer (React Hook Form),\ndisabling submit until the schema passes. The same schema shape is mirrored on the\nbackend so the two never drift apart silently.\nWhy: this is a UX layer, not a security layer. Its entire job is to give the user instant feedback\nand avoid wasting a round trip on a request that was always going to fail — it has zero\nbearing on data integrity. Anyone can bypass client-side validation with a raw HTTP call, so\nit must never be the thing standing between bad data and your database. That job belongs\nentirely to the backend, which is why the same schema exists on both sides but only one of\nthem is trusted.\n3. Backend validation and sanitization\nWhat: the real trust boundary. Every field is re-validated server-side regardless of what the\nclient already checked, and free-text fields are sanitized before they touch the database or\nget rendered anywhere.\nHow:\nSchema validation as Express middleware, per route, using the same Zod schema\nshape as the frontend — reject with 400 and a field-level error list before the handler\nruns.\nAll database access goes through parameterized queries (via pg or an ORM's query\nbuilder) — never string-concatenated SQL. This is what actually prevents SQL\ninjection; sanitization libraries are a second layer, not a substitute for\nparameterization.\nFree-text fields (if any — e.g. a display name) get trimmed and escaped with\nvalidator.js before storage.\nMoney is stored and computed as integer cents ( amount_cents: 45000 , not 450.00 ),\nnever as a float — floating-point arithmetic on currency is a real, avoidable source of\naccounting drift.\nWhy: the client cannot be trusted, full stop — not because your users are malicious by\ndefault, but because the request never has to come from your frontend at all. The backend\nschema is the actual gate; the frontend one is a convenience copy of it.\n4. Concurrency control — the seat-hold path\nThis is the highest-stakes write in the system, so it gets two layers with distinct jobs.",
      "num": 3
    },
    {
      "text": "sequenceDiagram\nparticipant A as User A\nparticipant B as User B\nparticipant R as Redis\nparticipant P as Postgres\nA->>R: SET seat:F12 NX EX ttl\nB->>R: SET seat:F12 NX EX ttl\nR-->>A: OK (lock acquired)\nR-->>B: fail — key exists\nNote over B: rejected in <1ms, no DB hit\nA->>P: BEGIN; SELECT status FOR UPDATE\nP-->>A: status = available\nA->>P: UPDATE status='held'; INSERT hold; COMMIT\nP-->>A: 200 — seat held\nWhat: exactly one of N concurrent requests for the same seat succeeds; all others are\nrejected cleanly with no partial state.\nHow — two layers, different responsibilities:\n1. Redis SET key NX EX <hold_ttl> — an atomic, sub-millisecond check. This is a\nperformance filter: under a burst of 100 requests for one seat, it rejects 99 of them\nbefore they ever reach the database, so Postgres only ever sees the request(s) that\nactually have a shot.\n2. Postgres SELECT ... FOR UPDATE inside a transaction — the correctness guarantee.\nThis locks the specific (showtime_id, seat_id) row; any second transaction targeting\nthe same row blocks until the first commits, then sees the updated status and is\nrejected. This holds even if Redis were ever wrong — a restart, an eviction, a bug —\nbecause the database constraint doesn't depend on Redis being correct.\nWhy two layers and not one: Redis alone is fast but not durable — nothing stops two\nrequests from both believing they hold the lock if Redis misbehaves. Postgres alone is\ncorrect but means every one of the 100 requests queues on the same row lock, which is fine\nfor a single seat but would strain the database under a genuinely large simultaneous rush\nacross many seats. Together, Redis absorbs the burst and Postgres guarantees correctness\neven in Redis's worst case.\nHold expiry: lazy expiry on read ( hold_expires_at < now() → treat as available) plus a\nBullMQ repeatable job sweeping expired holds back to available every few seconds.\nRedis's own key TTL expires in parallel, so a new SETNX on that seat naturally succeeds\nagain the moment the hold lapses — no manual coordination between the two expiries is\nneeded.",
      "num": 4
    },
    {
      "text": "5. Data integrity guarantees\nMechanism \tWhat it protects against\n(showtime_id, seat_id) composite\nprimary key\nTwo rows ever representing the same seat on the\nsame showtime\nSELECT ... FOR UPDATE transaction \tTwo bookings confirmed for one seat, even under\nconcurrent writers\nwebhook_events.event_id unique\nconstraint\nA duplicate gateway callback creating a second\npayment or confirming a booking twice\nInteger cents for all money fields \tFloating-point rounding drift in stored/reported\nrevenue\nSingle transaction for \"confirm booking +\nmark seat booked\"\nA seat marked booked with no corresponding\nbooking row, or vice versa, if the process crashes\nmid-write\ncreated_at / updated_at timestamps on\nbookings and payments\nNo audit trail to reconstruct what happened during\na disputed or duplicate transaction\nWhy a table: each of these is a single, boring mechanism doing one job. None of them\nrequires custom application logic to reason about — the guarantee comes from the database\nitself, which is more trustworthy than any amount of \"we checked for this in the handler\"\ncode.\n6. Payment — the async path\nWhat: /pay returns almost immediately; the actual outcome (success, failure, duplicate)\narrives later via a webhook the gateway calls back on.\nHow:\n1. Handler calls the gateway's /charge endpoint and returns 202 Pending to the client\nright away — it does not wait for the gateway, because the gateway's callback is\ndocumented as arriving 2–15 seconds later.\n2. The gateway calls POST /webhooks/gateway with the outcome. That handler does this,\nin order:\nsql",
      "num": 5
    },
    {
      "text": "No row returned → this event_id was already processed → return 200 and stop, doing\nnothing further. Row returned → first time seeing it → update payments.status and confirm\nthe booking in one transaction. 3. The handler always returns 200 , even for a duplicate —\nreturning anything else tells the gateway delivery failed, and it retries forever per the spec.\nWhy: the 8% duplicate-callback rate and 2–15 second delay aren't edge cases here, they're\nthe documented normal behavior of the gateway. Building the payment/booking\nconfirmation entirely around \"the callback might arrive twice or late, and my handler must\nbe safe either way\" means none of that behavior is a special case to handle — it's just the\nexpected shape of every request.\n7. Background processing\nWhat: two jobs run outside the request/response cycle — sweeping expired holds, and\nprocessing gateway webhooks that need more than a quick idempotency check.\nHow: BullMQ, backed by the same Redis instance (separate key namespace, bull:* ),\nrunning as a second process from the same codebase ( node worker.js vs node server.js ),\nboth in the same Docker image.\nWhy a separate process instead of doing this inline in the request handler: isolation. A\nslow or flaky gateway call recovering from a timeout shouldn't block or slow down someone\nbrowsing showtimes. Running it as its own process means the worst-case gateway\nmisbehavior degrades payment processing latency, not the rest of the app's responsiveness\n— you get part of the benefit people usually reach for microservices for, without actually\nneeding separate services.\n8. Keeping the UI honest\nWhat: the person who won the seat sees it confirmed instantly; everyone else looking at the\nsame seat map sees it flip to held/booked without needing a manual refresh.\nHow:\nThe acting user's own client updates optimistically the moment the hold request\nreturns 200 — it doesn't wait for anything else, since it already has the authoritative\nanswer.\nEveryone else's seat map polls the showtime's seat state every few seconds (a plain\nGET against the same read path used for the initial page load — no new\nINSERT INTO webhook_events (event_id, payment_id, received_at)\nVALUES ($1, $2, now())\nON CONFLICT (event_id) DO NOTHING\nRETURNING event_id;",
      "num": 6
    },
    {
      "text": "infrastructure).\nWhy polling over WebSockets here: a WebSocket/SSE layer gives you instant push\nupdates but adds real implementation and testing surface for an 8-hour build. Short polling\nis honest about the same information with a few seconds of staleness for passive viewers,\nwhile the person who actually competed for the seat — the one where correctness matters\n— never depends on polling at all, since their own request response is authoritative and\nimmediate. If time allows after the required milestones are solid, upgrading the seat map to\nSSE is a clean, isolated follow-up.\nQuick reference\nStep \tWhat \tHow \tWhy\nRequest entry Reject bad traffic early \tRate limit → auth → validate,\nin that order\nCheapest checks first\nFrontend\nvalidation\nFormat/completeness\nfeedback\nZod schema in the form\nlayer\nUX speed, not security\nBackend\nvalidation\nThe real gate \tSame schema, server-side,\nparameterized queries\nClient can't be trusted\nSeat hold \tExactly one winner \tRedis SETNX fast filter +\nPostgres FOR UPDATE\nSpeed + durability,\ntogether\nHold expiry \tSeats return to the pool \tLazy expiry + BullMQ sweep,\nRedis TTL in parallel\nNo manual\ncoordination needed\nPayment \tNever double-confirm \tFast 202 + idempotent\nwebhook via unique\nevent_id\nGateway is\ndocumented as\nunreliable\nBackground\njobs\nIsolate slow work \tSeparate BullMQ worker\nprocess, same Redis\nOne flaky call can't\nslow the whole app\nUI freshness \tReflect the real state \tOptimistic update for the\nactor, polling for viewers\nCorrectness doesn't\ndepend on push infra",
      "num": 7
    }
  ],
  "text": "CinemaSeat — architecture & data pipeline\nThis document explains what the system does at each stage, how it's implemented, and\nwhy that choice was made over the alternatives. It's written to double as the architecture\nsection of the hackathon README and as a reference for the team while building.\nStack: React (frontend) · Node.js / Express modular monolith (backend) · PostgreSQL\n(source of truth) · Redis (locks, OTP, rate limits, job queue) · BullMQ (background workers) ·\nprovided mock payment/OTP gateway.\n0. The one idea that repeats everywhere\nReject cheap, reject early. Every layer of this system is ordered so that the least expensive\ncheck happens first, and a request only reaches the expensive, contended resource\n(Postgres) after it has survived everything cheaper. You'll see this pattern three times: the\nmiddleware stack (rate limit → auth → validation, before any handler logic runs), the seat-\nhold flow (Redis lock before Postgres transaction), and the webhook handler (idempotency\ncheck before any business logic). It's the same principle applied at three different layers,\nand it's worth stating explicitly in DECISIONS.md — it's the reasoning that ties the whole\ndesign together.\n\n-- 1 of 7 --\n\n1. Request lifecycle — the full path of one HTTP request\nWhat: every request passes through the same ordered middleware chain before it ever\nreaches a handler. How: helmet (security headers) → cors → rate limiter (Redis-backed) →\nbody parser with a size cap → JWT auth check → route-specific schema\nvalidation/sanitization → handler. Why this order specifically: each step is cheaper than\nthe one after it. A rate-limited or unauthenticated request is rejected in microseconds\nwithout ever touching a validation schema or the database. Doing validation before auth, or\nauth before rate limiting, would mean paying the cost of the more expensive check for\ntraffic you were always going to reject anyway.\n2. Frontend validation\nWhat: format and completeness checks on forms — email/phone format, required fields,\nseat selection limits, OTP code shape — before a request is even sent.\nHTTPS request\nwithin limits\nvalid token\nschema valid\nenqueue job\nexceeded\ninvalid/missing token\nschema fails\nReact client\nRate limiter\nAuth middleware\nValidation and sanitization\nRoute handler / business\nlogic\nPostgreSQL \tRedis BullMQ worker\n429 Too Many Requests\n401 Unauthorized\n400 Bad Request\n\n-- 2 of 7 --\n\nHow: schema-based validation (Zod or Yup) wired into the form layer (React Hook Form),\ndisabling submit until the schema passes. The same schema shape is mirrored on the\nbackend so the two never drift apart silently.\nWhy: this is a UX layer, not a security layer. Its entire job is to give the user instant feedback\nand avoid wasting a round trip on a request that was always going to fail — it has zero\nbearing on data integrity. Anyone can bypass client-side validation with a raw HTTP call, so\nit must never be the thing standing between bad data and your database. That job belongs\nentirely to the backend, which is why the same schema exists on both sides but only one of\nthem is trusted.\n3. Backend validation and sanitization\nWhat: the real trust boundary. Every field is re-validated server-side regardless of what the\nclient already checked, and free-text fields are sanitized before they touch the database or\nget rendered anywhere.\nHow:\nSchema validation as Express middleware, per route, using the same Zod schema\nshape as the frontend — reject with 400 and a field-level error list before the handler\nruns.\nAll database access goes through parameterized queries (via pg or an ORM's query\nbuilder) — never string-concatenated SQL. This is what actually prevents SQL\ninjection; sanitization libraries are a second layer, not a substitute for\nparameterization.\nFree-text fields (if any — e.g. a display name) get trimmed and escaped with\nvalidator.js before storage.\nMoney is stored and computed as integer cents ( amount_cents: 45000 , not 450.00 ),\nnever as a float — floating-point arithmetic on currency is a real, avoidable source of\naccounting drift.\nWhy: the client cannot be trusted, full stop — not because your users are malicious by\ndefault, but because the request never has to come from your frontend at all. The backend\nschema is the actual gate; the frontend one is a convenience copy of it.\n4. Concurrency control — the seat-hold path\nThis is the highest-stakes write in the system, so it gets two layers with distinct jobs.\n\n-- 3 of 7 --\n\nsequenceDiagram\nparticipant A as User A\nparticipant B as User B\nparticipant R as Redis\nparticipant P as Postgres\nA->>R: SET seat:F12 NX EX ttl\nB->>R: SET seat:F12 NX EX ttl\nR-->>A: OK (lock acquired)\nR-->>B: fail — key exists\nNote over B: rejected in <1ms, no DB hit\nA->>P: BEGIN; SELECT status FOR UPDATE\nP-->>A: status = available\nA->>P: UPDATE status='held'; INSERT hold; COMMIT\nP-->>A: 200 — seat held\nWhat: exactly one of N concurrent requests for the same seat succeeds; all others are\nrejected cleanly with no partial state.\nHow — two layers, different responsibilities:\n1. Redis SET key NX EX <hold_ttl> — an atomic, sub-millisecond check. This is a\nperformance filter: under a burst of 100 requests for one seat, it rejects 99 of them\nbefore they ever reach the database, so Postgres only ever sees the request(s) that\nactually have a shot.\n2. Postgres SELECT ... FOR UPDATE inside a transaction — the correctness guarantee.\nThis locks the specific (showtime_id, seat_id) row; any second transaction targeting\nthe same row blocks until the first commits, then sees the updated status and is\nrejected. This holds even if Redis were ever wrong — a restart, an eviction, a bug —\nbecause the database constraint doesn't depend on Redis being correct.\nWhy two layers and not one: Redis alone is fast but not durable — nothing stops two\nrequests from both believing they hold the lock if Redis misbehaves. Postgres alone is\ncorrect but means every one of the 100 requests queues on the same row lock, which is fine\nfor a single seat but would strain the database under a genuinely large simultaneous rush\nacross many seats. Together, Redis absorbs the burst and Postgres guarantees correctness\neven in Redis's worst case.\nHold expiry: lazy expiry on read ( hold_expires_at < now() → treat as available) plus a\nBullMQ repeatable job sweeping expired holds back to available every few seconds.\nRedis's own key TTL expires in parallel, so a new SETNX on that seat naturally succeeds\nagain the moment the hold lapses — no manual coordination between the two expiries is\nneeded.\n\n-- 4 of 7 --\n\n5. Data integrity guarantees\nMechanism \tWhat it protects against\n(showtime_id, seat_id) composite\nprimary key\nTwo rows ever representing the same seat on the\nsame showtime\nSELECT ... FOR UPDATE transaction \tTwo bookings confirmed for one seat, even under\nconcurrent writers\nwebhook_events.event_id unique\nconstraint\nA duplicate gateway callback creating a second\npayment or confirming a booking twice\nInteger cents for all money fields \tFloating-point rounding drift in stored/reported\nrevenue\nSingle transaction for \"confirm booking +\nmark seat booked\"\nA seat marked booked with no corresponding\nbooking row, or vice versa, if the process crashes\nmid-write\ncreated_at / updated_at timestamps on\nbookings and payments\nNo audit trail to reconstruct what happened during\na disputed or duplicate transaction\nWhy a table: each of these is a single, boring mechanism doing one job. None of them\nrequires custom application logic to reason about — the guarantee comes from the database\nitself, which is more trustworthy than any amount of \"we checked for this in the handler\"\ncode.\n6. Payment — the async path\nWhat: /pay returns almost immediately; the actual outcome (success, failure, duplicate)\narrives later via a webhook the gateway calls back on.\nHow:\n1. Handler calls the gateway's /charge endpoint and returns 202 Pending to the client\nright away — it does not wait for the gateway, because the gateway's callback is\ndocumented as arriving 2–15 seconds later.\n2. The gateway calls POST /webhooks/gateway with the outcome. That handler does this,\nin order:\nsql\n\n-- 5 of 7 --\n\nNo row returned → this event_id was already processed → return 200 and stop, doing\nnothing further. Row returned → first time seeing it → update payments.status and confirm\nthe booking in one transaction. 3. The handler always returns 200 , even for a duplicate —\nreturning anything else tells the gateway delivery failed, and it retries forever per the spec.\nWhy: the 8% duplicate-callback rate and 2–15 second delay aren't edge cases here, they're\nthe documented normal behavior of the gateway. Building the payment/booking\nconfirmation entirely around \"the callback might arrive twice or late, and my handler must\nbe safe either way\" means none of that behavior is a special case to handle — it's just the\nexpected shape of every request.\n7. Background processing\nWhat: two jobs run outside the request/response cycle — sweeping expired holds, and\nprocessing gateway webhooks that need more than a quick idempotency check.\nHow: BullMQ, backed by the same Redis instance (separate key namespace, bull:* ),\nrunning as a second process from the same codebase ( node worker.js vs node server.js ),\nboth in the same Docker image.\nWhy a separate process instead of doing this inline in the request handler: isolation. A\nslow or flaky gateway call recovering from a timeout shouldn't block or slow down someone\nbrowsing showtimes. Running it as its own process means the worst-case gateway\nmisbehavior degrades payment processing latency, not the rest of the app's responsiveness\n— you get part of the benefit people usually reach for microservices for, without actually\nneeding separate services.\n8. Keeping the UI honest\nWhat: the person who won the seat sees it confirmed instantly; everyone else looking at the\nsame seat map sees it flip to held/booked without needing a manual refresh.\nHow:\nThe acting user's own client updates optimistically the moment the hold request\nreturns 200 — it doesn't wait for anything else, since it already has the authoritative\nanswer.\nEveryone else's seat map polls the showtime's seat state every few seconds (a plain\nGET against the same read path used for the initial page load — no new\nINSERT INTO webhook_events (event_id, payment_id, received_at)\nVALUES ($1, $2, now())\nON CONFLICT (event_id) DO NOTHING\nRETURNING event_id;\n\n-- 6 of 7 --\n\ninfrastructure).\nWhy polling over WebSockets here: a WebSocket/SSE layer gives you instant push\nupdates but adds real implementation and testing surface for an 8-hour build. Short polling\nis honest about the same information with a few seconds of staleness for passive viewers,\nwhile the person who actually competed for the seat — the one where correctness matters\n— never depends on polling at all, since their own request response is authoritative and\nimmediate. If time allows after the required milestones are solid, upgrading the seat map to\nSSE is a clean, isolated follow-up.\nQuick reference\nStep \tWhat \tHow \tWhy\nRequest entry Reject bad traffic early \tRate limit → auth → validate,\nin that order\nCheapest checks first\nFrontend\nvalidation\nFormat/completeness\nfeedback\nZod schema in the form\nlayer\nUX speed, not security\nBackend\nvalidation\nThe real gate \tSame schema, server-side,\nparameterized queries\nClient can't be trusted\nSeat hold \tExactly one winner \tRedis SETNX fast filter +\nPostgres FOR UPDATE\nSpeed + durability,\ntogether\nHold expiry \tSeats return to the pool \tLazy expiry + BullMQ sweep,\nRedis TTL in parallel\nNo manual\ncoordination needed\nPayment \tNever double-confirm \tFast 202 + idempotent\nwebhook via unique\nevent_id\nGateway is\ndocumented as\nunreliable\nBackground\njobs\nIsolate slow work \tSeparate BullMQ worker\nprocess, same Redis\nOne flaky call can't\nslow the whole app\nUI freshness \tReflect the real state \tOptimistic update for the\nactor, polling for viewers\nCorrectness doesn't\ndepend on push infra\n\n-- 7 of 7 --\n\n",
  "total": 7
}